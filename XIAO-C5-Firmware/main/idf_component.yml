# No managed components on the native-AT-HTTP transport.
# (The PPP branch pulls espressif/esp_modem here; this branch talks to the
# SIM7600 over UART with the modem's native AT HTTP(S) stack and needs none.)
dependencies: {}
